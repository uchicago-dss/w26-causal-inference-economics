# Thesis Notebooks

## Primary workflow (recommended)

Run these in order:

1. `01_build_did_panel.qmd`
2. `02_did_regression_ladder.qmd`
3. `03_event_study.qmd`
4. `04_seasonality_sanity_checks.qmd`

These write canonical outputs to `analysis/results/`.

## Legacy full pipeline

- `thesis_full_pipeline.qmd` is preserved as a monolithic reference workflow.
- It is intentionally kept unchanged.
- It now writes generated data/tables/figures to `analysis/results/` (matching the modular notebooks).

## Rendered outputs

Rendered `.pdf` files are generated artifacts and can be re-rendered from `.qmd` sources.
